# Comandos utiles git


1-   git init                     ( empezar repo )
2-   git add .                    ( añadir todos los archivos )
3-   git reset .                  ( quitar todos los archivos )
4-   git commit -m  "la m es para comentarios"
5-   git checkout -- .            ( reconstruir archivos al ultimo commit )
6-   git log  
7-   git commit --amend           ( editar ultimo commit, :wp!  para salir del editor ) 
8-   git checkout -b nombre_rama  ( crear rama )
9-   git branch                   ( ver ramas )
10-  git checkout master          ( cambiar a rama master )
11-  git merge nombre-rama        ( fusionar rama con la rama activa )  :wp! para salir
12-  git branch -d nombre_rama   ( borrar rama )
13.  git push                      
14.  git commit -am "agregado"    ( git commit + git add )